# ClipboardTool

Windows 剪贴板历史工具：后台记录复制过的文字与图片，用全局快捷键呼出一个不抢焦点的浮层面板，选中后把内容粘贴回原来的输入框。

本文件是**词汇表**：只记这个项目特有的说法、它们的边界，以及该避开哪些含糊词。实现地图见 [architecture.md](docs/architecture.md)，难回退的决策见 [adr/](docs/adr/)，当次改动史见 [changelog.md](docs/changelog.md)，用法与构建见 [README.md](README.md)。

## Language

### 历史与条目

**历史条目（Entry）**：
一条被记录的复制结果，类型是文字或图片。
_Avoid_: 记录、卡片、item（渲染层叫 `ClipboardEntry`，是同一物的前端名）

**条目身份**：
判定「两次复制是不是同一条」的唯一规则：只看内容。
_Avoid_: 去重键、指纹（指纹只指图片那半边的实现）

**落位**：
一条条目在历史列表里的最终位置，由置顶状态与最近使用共同决定。
_Avoid_: 排序、移动

**置顶块 / 普通块**：
列表的两段布局：置顶条目形成的连续头部块，与其后按最近使用排列的普通条目段。
_Avoid_: 收藏区、固定区

**置顶时间（pinnedAt）**：
最近一次置顶的时间戳，置顶块内部按它新→旧排；未置顶为 0。

**来源应用**：
复制发生时前台应用的身份：可执行文件路径、应用名、窗口标题、图标。

**备注（note）**：
用户附加在条目上的可选单行文本，独立于剪贴板正文与来源应用，不参与条目身份判定；空字符串即没有备注。

### 面板与模式

**面板**：
呼出的那个不可激活浮层，显示历史并接收键盘导航。
_Avoid_: 窗口、弹窗、下拉框

**呼出**：
按全局键让面板出现在光标处，同时记录焦点快照。
_Avoid_: 打开、显示（这两个词不含「记录快照」那层含义）

**停靠（park）**：
面板「关闭」时的实际动作：移到当前显示器工作区之外驻留，而不是销毁窗口。
_Avoid_: 隐藏、最小化

**面板模式**：
面板显示期间的四种输入状态：浏览态、搜索态、备注编辑态、捕获态；后三者合称**输入态**。
_Avoid_: 子模式、状态标志

**呼出键 / 导航键**：
两类全局热键。呼出键任何时候都注册；导航键只在面板显示期间注册，且其集合由当前面板模式推导。
_Avoid_: 快捷键（两者混称会丢掉「谁在什么时候让位」这条规则）

**让位**：
某个键从导航键集合里退出、交还给输入框，例如搜索态下的空格、`Z`、`Del`。
_Avoid_: 注销、禁用（注销是插件动作，让位是规则）

**焦点快照（focusTarget）**：
呼出时记下的前台窗口与焦点控件组合，把内容粘贴回原输入框靠它。

**捕获态**：
等待用户按下新呼出键的面板模式，期间其余热键全部让位。

**保持选中项可见**：
键盘导航的一条不变式：选中项任何时候都完整落在可视区内，滚到边缘时滚到的是渐隐遮罩内侧，不是滚动口边缘。
_Avoid_: 自动滚动、滚动跟随（都不含「不许贴进遮罩」这层约束）

**可视区**：
滚动容器里真正能看的矩形：滚动口再让开上下各一段渐隐宽度。贴到滚动口边缘不算可见。
_Avoid_: 视口（本项目里「视口」指整个面板窗口，与滚动容器的可视区不是一回事）

**渐隐遮罩**：
列表上下边缘各 10px 的 mask 淡出，用来代替硬分割线。
_Avoid_: 阴影（设计语言禁用阴影）、蒙版

**滚动口**：
滚动容器自己的内容框，不含为渐隐让出的那段留白；`scrollIntoView` 默认只认它。

### 粘贴链路

**复制并粘贴**：
本项目的一个完整动作：写剪贴板，并把内容注入回原输入框。
_Avoid_: 复制（单说「复制」时只指写剪贴板那半步）

**注入**：
向已恢复焦点的原窗口发送 `Ctrl+V`。
_Avoid_: 粘贴（与「复制并粘贴」这个整体动作同词，容易混）

**轮询基线**：
主进程「已经见过的剪贴板内容」，用来判定下一轮轮询算不算一次新复制。
_Avoid_: 缓存、快照（快照一词已归焦点快照）

**新复制**：
与轮询基线不同、因此值得记进历史的一次剪贴板变化；自己刚写进剪贴板的内容不算。

**结果契约（CopyResult）**：
复制并粘贴返回给渲染层的 `{ ok, message }`，键盘 Enter、双击、复制按钮三个入口共用。
_Avoid_: 成功失败标志（漏掉「文案单一来源」这层）

**失败阶段（stage）**：
粘贴链路的两种失败：`restore` 没能恢复原窗口焦点，`paste` 恢复了但注入没成。两者文案不同，且都决定面板保持显示。

### 提权与启动

**常驻提权**：
应用始终以高完整性运行这一设计前提；因此工具不存在「未提权」这个用户可见状态。
_Avoid_: 提权模式、管理员开关

**静默启动通道**：
计划任务 `ClipboardToolElevated`：以管理员令牌直接创建进程，把 UAC 同意只留在任务创建那一刻。
_Avoid_: 计划任务（开机启动用的是同一个任务的另一副触发器，单说「计划任务」分不清指哪个）

**意图 / 事实**：
开机启动的两面：用户想要的（持久化的 `autoStart`）与系统当前的（任务及其触发器是否真的存在）。二者可以不一致，靠开关或下次提权启动收敛。
_Avoid_: 期望值、实际值

**通道三态**：
判定开机启动能否落地的三种运行处境：开发构建、未提权、已提权。

### 存档与契约

**存档**：
`%APPDATA%\ClipboardTool` 下的全部用户数据：历史 JSON、`images/`、`settings.json`。沿用 Electron 版契约，两版不要同时运行。

**键名契约**：
`settings.json` 用 camelCase 键名，因为这份存档与 Electron 版共享；序列化键名一改就是存档破坏。
